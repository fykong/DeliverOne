# 复制本文件为 .env 并填入真实值（.env 已被 gitignore，不会被提交）

# 火山方舟 doubao-seed-2.0-lite（比赛统一下发的 API Key 与 EP id，均严禁外泄）
ARK_API_KEY=
ARK_MODEL=

# 可选：GitHub Token（用于提测链路自动创建 PR；不配置时交付为 PR-ready 分支 + patch）
# GITHUB_TOKEN=
