# 复制本文件为 .env 并填入真实值（.env 已被 gitignore，不会被提交）

# 火山方舟 doubao-seed-2.0-lite（比赛统一下发的 API Key）
ARK_API_KEY=

# 可选：覆盖 EP id（默认使用 config/model-providers.json 的 model 字段）
# ARK_MODEL=EP_ID_REDACTED

# 可选：GitHub Token（用于提测链路自动创建 PR；不配置时交付为 PR-ready 分支 + patch）
# GITHUB_TOKEN=
