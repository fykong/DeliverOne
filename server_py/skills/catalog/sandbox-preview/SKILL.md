# 沙盒预览 Skill

## 触发场景

用户需要实时预览、启动页面、查看运行效果、打开浏览器或验证 UI 时使用。

## 流程

1. 确认当前对话已有沙盒仓库。
2. 识别可用启动命令。
3. 用户点击或确认后，在沙盒仓库内启动命令。
4. 记录进程 id、端口、stdout、stderr。
5. 后续接入浏览器 smoke test 和截图证据。

## 限制

- 不在原始仓库启动命令。
- 非可信命令必须请求确认。
- 预览失败时必须显示 stderr 摘要，不用空白 iframe 模糊用户。
